'use client';
import {useState} from 'react'; import {ChevronRight} from 'lucide-react'; import type {Application,ApplicationStatus} from '@/lib/types'; import {Button} from './ui';
export function ApplicationAction({app,next,onAdvance}:{app:Application;next:ApplicationStatus;onAdvance:(id:string,status:ApplicationStatus)=>void}){const [confirm,setConfirm]=useState(false);const needsConfirm=next==='Applied';function click(){if(needsConfirm)setConfirm(true);else onAdvance(app.id,next)}return <div className="relative inline-flex"><Button variant="ghost" onClick={click} className="h-8 min-h-8 px-2.5">{next}<ChevronRight size={13}/></Button>{confirm&&<div className="elevated absolute right-0 top-[calc(100%+8px)] z-30 w-72 p-3 text-left"><div className="text-13 font-medium">Confirm applied status</div><p className="mt-1 text-[12px] leading-5 text-text-secondary">Confirm you submitted this application outside CareerPilot. The status changes only after you approve it.</p><div className="mt-3 flex justify-end gap-2"><Button variant="ghost" onClick={()=>setConfirm(false)} className="h-8 min-h-8">Cancel</Button><Button onClick={()=>{onAdvance(app.id,next);setConfirm(false)}} className="h-8 min-h-8">Confirm applied</Button></div></div>}</div>}
