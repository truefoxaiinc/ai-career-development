'use client';
import {useRef,useState} from 'react';import {Upload,FileText,RefreshCw} from 'lucide-react';import {Button,ErrorState,Panel,ProgressBar} from '@/components/ui';import {useResumeUpload} from '@/hooks/useResumeStudio';import type {StudioResume} from '@/lib/resume-studio';
const fmt=(n:number)=>n<1024*1024?`${Math.ceil(n/1024)} KB`:`${(n/1024/1024).toFixed(1)} MB`;
export function ResumeUpload({resumes,maxBytes,onSelect}:{resumes:StudioResume[];maxBytes:number;onSelect:(id:string)=>void}){const input=useRef<HTMLInputElement>(null);const [progress,setProgress]=useState(0);const [validation,setValidation]=useState('');const upload=useResumeUpload(setProgress);function choose(file?:File){if(!file)return;setValidation('');if(file.size>maxBytes){setValidation(`File exceeds the ${Math.floor(maxBytes/1024/1024)} MB limit.`);return}if(!/\.(pdf|docx)$/i.test(file.name)){setValidation('Choose a PDF or DOCX file.');return}setProgress(0);upload.mutate(file,{onSuccess:r=>onSelect(r.id)})}return <section aria-labelledby="resume-upload-title"><Panel className="p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 id="resume-upload-title" className="text-18 font-semibold">Resume uploads</h2><p className="mt-1 text-13 text-text-secondary">PDF or DOCX, up to {Math.floor(maxBytes/1024/1024)} MB. Replacements are saved as new versions.</p></div><><input ref={input} className="sr-only" type="file" accept=".pdf,.docx,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document" onChange={e=>choose(e.target.files?.[0])}/><Button onClick={()=>input.current?.click()} disabled={upload.isPending}>{resumes.length?<RefreshCw size={15}/>:<Upload size={15}/>} {upload.isPending?'Uploading…':resumes.length?'Upload new version':'Upload resume'}</Button></></div>{upload.isPending&&<div className="mt-4" aria-live="polite"><ProgressBar value={progress} label="Upload progress"/></div>}{(validation||upload.error)&&<div className="mt-4"><ErrorState message={validation||upload.error?.message||'Upload failed'} retry={()=>input.current?.click()}/></div>}<div className="mt-5 grid gap-2">{resumes.map(r=><button key={r.id} onClick={()=>onSelect(r.id)} className="flex w-full items-center gap-3 rounded-card border border-border bg-surface-2 p-3 text-left focus:outline-none focus:ring-2 focus:ring-accent"><FileText size={18}/><span className="min-w-0 flex-1"><span className="block truncate text-13 font-medium">{r.filename}</span><span className="block text-[12px] text-text-secondary">Version {r.version} · {fmt(r.size_bytes)} · {new Date(r.uploaded_at).toLocaleString()}</span></span><span className="font-mono text-[12px] capitalize text-text-secondary" role="status">{r.status}{r.task&&r.status!=='completed'?` ${r.task.progress}%`:''}</span></button>)}</div></Panel></section>}
